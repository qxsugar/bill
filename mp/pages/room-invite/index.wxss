.container { padding: var(--spacing-base); }
.code-block { text-align: center; margin: var(--spacing-lg) 0; }
.code { font-size: var(--font-size-xxl); font-weight: bold; color: var(--color-primary); letter-spacing: 8rpx; }
.qr-img { width: 400rpx; height: 400rpx; }
