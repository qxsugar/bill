.container { padding: var(--spacing-base); text-align: center; }
.success-icon { font-size: 120rpx; color: var(--color-secondary); margin-top: var(--spacing-xl); }
.success-text { display: block; font-size: var(--font-size-xl); margin: var(--spacing-base) 0 var(--spacing-xl); }
